{
  "name": "cambridge-one-automation",
  "version": "1.0.0",
  "description": "Playwright POM automation for Cambridge One (micro-nemo)",
  "scripts": {
    "test": "npx playwright test",
    "test:headed": "npx playwright test --headed",
    "report": "npx playwright show-report",
    "agent": "node tests/createClass-agent.js",
    "verify:homepage": "node tests/verifyHomepage-agent.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.36.3",
    "dotenv": "^17.4.2",
    "nodemailer": "^8.0.7",
    "playwright": "^1.59.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1"
  }
}
